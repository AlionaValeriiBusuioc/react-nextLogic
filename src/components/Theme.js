import { createTheme } from '@mui/material/styles';
const greyBackground = "#dbdde1";
const blackColor = "#161819";

export default createTheme({
    palette: {
        common: {
          grey: greyBackground,
          black: blackColor
        }
      }
});